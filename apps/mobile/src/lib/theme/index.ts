/**
 * Owns the mobile Tamagui entrypoint.
 * Shared design-token semantics are applied in `tamaguiThemes.ts`;
 * feature code should import theme values from Tamagui rather than bypassing this module.
 */
export { tamaguiConfig } from "../../../tamagui.config";
