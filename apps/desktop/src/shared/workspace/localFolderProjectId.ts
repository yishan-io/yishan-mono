/**
 * Sentinel project id for local (non-git) folder workspaces (desktop8
 * Phase 30: moved out of the Project Model so Workspace and App code can
 * share the constant without a cross-Domain Model import).
 */
export const LOCAL_FOLDER_PROJECT_ID = "local-folder";
