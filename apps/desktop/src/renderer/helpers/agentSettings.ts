/**
 * Compatibility re-export — agent settings model now lives under the Agent
 * feature (Phase 19). Import from `@renderer/domains/agent` directly.
 */
export * from "../domains/agent/model/agentSettings";
