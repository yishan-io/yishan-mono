/**
 * Compatibility re-export — agent settings model now lives under the Agent
 * feature (Phase 19). Import from `@renderer/features/agent` directly.
 */
export * from "../features/agent/model/agentSettings";
