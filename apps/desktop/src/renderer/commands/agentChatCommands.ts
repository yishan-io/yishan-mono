/**
 * @deprecated Temporary facade — Agent Chat command surface moved to
 * `features/agent/commands/agentChatCommands.ts` (refactor Phase 5).
 * Migrate importers; delete this file after all callers move (Task 6).
 */
export * from "../features/agent/commands/agentChatCommands";
