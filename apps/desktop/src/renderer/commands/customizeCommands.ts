/**
 * @deprecated Temporary facade — moved to
 * `features/settings/commands/customizeCommands.ts` (refactor Phase 8).
 * Migrate importers; delete this file after all callers move (Task 6).
 */
export * from "../features/settings/commands/customizeCommands";
