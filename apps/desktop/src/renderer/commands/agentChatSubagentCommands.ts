/**
 * @deprecated Temporary facade — moved to
 * `features/agent/commands/agentChatSubagentCommands.ts` (refactor Phase 5).
 * Migrate importers; delete this file after all callers move (Task 6).
 */
export * from "../features/agent/commands/agentChatSubagentCommands";
