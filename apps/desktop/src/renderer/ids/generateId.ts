/**
 * Renderer ID capability (desktop7 Phase 26).
 *
 * Re-exports `generateId` from the shared module so the renderer never
 * imports the shared helpers path directly.
 */
export { generateId } from "../../shared/helpers/generateId";
